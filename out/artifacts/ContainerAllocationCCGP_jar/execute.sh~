#!/bin/tcsh 

#if(-e /local/tmp/tanboxi) then
	#rm /local/tmp/tanboxi/ -rf
	#endif
	#
	#if('$1' == '') then
		#echo 'Usage ./execute.sh [param]'
		#exit
		#endif

		#mkdir /local/tmp/tanboxi/ 
		#mkdir /local/tmp/tanboxi/VMCreationGP/
java -cp ./ContainerAllocationCCGP.jar main.Main $1 $2 &
